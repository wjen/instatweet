<%= link_to gravatar_for(current_user, size: 120), current_user %>
<h1 class="profile"><%= current_user.handle %></h1>
<span><%= link_to "View my profile", current_user %></span> |
<span><%= pluralize(current_user.tweets.count, "tweet") %> </span>
